
const numbers = [1, 2, 3];

//remove at End Of array
const last = numbers.pop();

//remove at the start of array
const start = numbers.shift();

//remove at mid
const mid = numbers.splice(2, 1); //2 is index, 1 is number of elements to be removed 
