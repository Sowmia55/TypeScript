const number = [1, 2, 3];

const joined = numbers.join(",");
console.log(joined); //1,2,3

const message = "This is my original message"
const parts = message.split(" ");
console.log(parts);

const joined2 = parts.join("-");
console.log(joined); //This-is-my-original-message